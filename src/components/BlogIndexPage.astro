---
import Layout from '../layouts/Layout.astro';
import Navbar from './Navbar.astro';
import SiteFooter from './SiteFooter.astro';
import { blogUi, type LocaleCode, defaultLocale } from '../data/i18n/locales';
import {
	absoluteBlogUrl,
	getAllPostsForLocale,
	getBlogBasePath,
	getFeaturedPosts,
	getBlogIndexHreflangAlternates,
} from '../data/blog/helpers';
import { getCheatBlogLinks } from '../data/blog/internal-links';
import { OFFICIAL_WARZONE_RESOURCES } from '../data/external-resources';
import ExternalResourcesBlock from './ExternalResourcesBlock.astro';
import { blogLabel, siteConfig } from '../data/site';
import { brandSeo, seoPageTitle } from '../data/site-core';
import { seoDescription } from '../data/brand';
import { getLocalizedPath } from '../data/i18n/routing';
import { getT } from '../i18n/server';

interface Props {
	locale: LocaleCode;
}

const { locale } = Astro.props;
const ui = blogUi[locale];
const t = getT(locale);
const posts = getAllPostsForLocale(locale);
const featured = getFeaturedPosts(locale, 3);
const cheatGuides = getCheatBlogLinks(locale);
const canonicalPath = getBlogBasePath(locale);
const canonicalURL = absoluteBlogUrl(locale);
const hreflangAlternates = getBlogIndexHreflangAlternates(locale);
const homeHref = getLocalizedPath('home', locale);

const pageTitle = locale === defaultLocale ? seoPageTitle(brandSeo.blogTitle) : ui.blogTitle;
const pageDescription =
	locale === defaultLocale ? seoDescription(brandSeo.blogDescription) : ui.blogDescription;

const articleListSchema = {
	'@type': 'CollectionPage',
	'@id': `${canonicalURL}#collection`,
	name: ui.blogH1,
	description: pageDescription,
	url: canonicalURL,
	inLanguage: locale,
	isPartOf: { '@id': `${siteConfig.url}/#website` },
	mainEntity: {
		'@type': 'ItemList',
		itemListElement: posts.map((post, i) => ({
			'@type': 'ListItem',
			position: i + 1,
			url: absoluteBlogUrl(locale, post.translation.slug),
			name: post.translation.title,
		})),
	},
};
---

<Layout
	title={pageTitle}
	description={pageDescription}
	canonicalPath={canonicalPath}
	hreflangAlternates={hreflangAlternates}
	locale={locale}
	ogImage={siteConfig.defaultOgImage}
	ogImageAlt={ui.blogH1}
	breadcrumbs={[{ label: ui.home, href: homeHref }, { label: ui.blogH1 }]}
	extraGraph={[articleListSchema]}
>
	<Navbar locale={locale} />
	<main class="blog">
		<div class="shell blog__inner">
		<header class="blog__hero">
			<p class="blog__eyebrow" data-edit="blogLabel">{blogLabel}</p>
			<h1>{ui.blogH1}</h1>
			<p class="blog__intro">{ui.blogIntro}</p>
		</header>

		{
			cheatGuides.length > 0 && (
				<section class="blog__cheat-hub" aria-labelledby="blog-cheat-guides-title">
					<header class="blog__cheat-hub-head">
						<h2 id="blog-cheat-guides-title">{t('blog.cheatGuidesTitle')}</h2>
						<p>{t('blog.cheatGuidesLede')}</p>
					</header>
					<ul class="blog__cheat-hub-list">
						{cheatGuides.map((item) => (
							<li>
								<a href={item.href}>
									<span class="blog__cat">{item.category}</span>
									<span>{item.title}</span>
								</a>
							</li>
						))}
					</ul>
					<p class="blog__cheat-hub-links">
						<a href="/warzone-cheats/">{t('blog.cheatGuidesPillar')}</a>
						·
						<a href="/pricing/">{t('blog.cheatGuidesPricing')}</a>
						·
						<a href="/updates/">{t('blog.cheatGuidesStatus')}</a>
					</p>
				</section>
			)
		}

		{
			featured.length > 0 && (
				<section class="blog__featured" aria-label={t('common.featured')}>
					{
						featured.map((post, index) => (
							<a class="blog__feature-card" href={post.canonicalPath}>
								<img
									src={post.imageSrc}
									alt={post.translation.imageAlt}
									width="640"
									height="360"
									loading={index === 0 ? 'eager' : 'lazy'}
									decoding="async"
									fetchpriority={index === 0 ? 'high' : undefined}
								/>
								<div>
									<span class="blog__cat">{post.category}</span>
									<h2>{post.translation.title}</h2>
									<p>{post.translation.intro}</p>
									<span class="blog__more">{ui.readMore}</span>
								</div>
							</a>
						))
					}
				</section>
			)
		}

		<section class="blog__list" aria-label={ui.allPosts}>
			{
				posts.map((post) => (
					<article class="blog__card">
						<a href={post.canonicalPath}>
							<img
								src={post.imageSrc}
								alt={post.translation.imageAlt}
								width="480"
								height="270"
								loading="lazy"
								decoding="async"
							/>
							<span class="blog__cat">{post.category}</span>
							<h2>{post.translation.title}</h2>
							<p>{post.translation.intro}</p>
							<span class="blog__meta">
								{ui.updated} {post.updated}
							</span>
						</a>
					</article>
				))
			}
		</section>
		</div>
	</main>
	<div class="shell blog__resources-wrap">
		<ExternalResourcesBlock
			title={t('externalResources.title')}
			lede={t('externalResources.blogLede')}
			resources={OFFICIAL_WARZONE_RESOURCES}
		/>
	</div>
	<SiteFooter locale={locale} />
</Layout>

<style>
	.blog {
		padding: 2.5rem 0 4rem;
	}

	.blog__resources-wrap {
		padding-bottom: 3rem;
	}

	.blog__inner {
		display: grid;
		gap: 0;
	}
	.blog__hero {
		margin-bottom: 2.5rem;
	}
	.blog__eyebrow {
		margin: 0 0 0.5rem;
		color: var(--magenta-soft);
		font-weight: 700;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		font-size: 0.75rem;
	}
	.blog__hero h1 {
		margin: 0 0 0.75rem;
		font-size: clamp(2.2rem, 5vw, 3.4rem);
		line-height: 1.05;
	}
	.blog__intro {
		max-width: 42rem;
		color: rgba(255, 255, 255, 0.72);
		font-size: 1.05rem;
	}
	.blog__cheat-hub {
		margin-bottom: 2.5rem;
		padding: 1.25rem 1.35rem;
		border: 1px solid var(--line);
		border-radius: var(--radius-md, 0.75rem);
		background: var(--bg-panel);
	}
	.blog__cheat-hub-head h2 {
		margin: 0 0 0.5rem;
		font-size: 1.25rem;
	}
	.blog__cheat-hub-head p {
		margin: 0 0 1rem;
		color: rgba(255, 255, 255, 0.68);
		font-size: 0.92rem;
		max-width: 42rem;
	}
	.blog__cheat-hub-list {
		margin: 0;
		padding: 0;
		list-style: none;
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 0.45rem;
	}
	.blog__cheat-hub-list a {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 0.65rem;
		align-items: center;
		padding: 0.75rem 0.9rem;
		border: 1px solid var(--line-soft);
		border-radius: var(--radius-md, 0.75rem);
		background: var(--bg-elevated);
		text-decoration: none;
		color: inherit;
		font-weight: 600;
		font-size: 0.9rem;
		transition: border-color 0.15s ease, color 0.15s ease;
	}
	.blog__cheat-hub-list a:hover {
		border-color: color-mix(in srgb, var(--magenta) 35%, var(--line));
		color: var(--magenta-soft);
	}
	.blog__cheat-hub-links {
		margin: 1rem 0 0;
		font-size: 0.85rem;
		color: rgba(255, 255, 255, 0.55);
	}
	.blog__cheat-hub-links a {
		color: var(--magenta-soft);
		text-decoration: none;
		font-weight: 600;
	}
	.blog__cheat-hub-links a:hover {
		text-decoration: underline;
	}
	.blog__featured {
		display: grid;
		gap: 1.25rem;
		margin-bottom: 2.5rem;
	}
	.blog__feature-card {
		display: grid;
		grid-template-columns: minmax(0, 1.1fr) minmax(0, 1.2fr);
		gap: 1.25rem;
		padding: 1rem;
		border: 1px solid var(--line);
		border-radius: var(--radius-md, 0.75rem);
		background: var(--bg-panel);
		text-decoration: none;
		color: inherit;
	}
	.blog__feature-card img {
		width: 100%;
		height: 100%;
		max-height: 240px;
		object-fit: cover;
	}
	.blog__feature-card h2 {
		margin: 0.4rem 0 0.6rem;
		font-size: 1.35rem;
	}
	.blog__list {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(min(100%, 240px), 1fr));
		gap: 1.25rem;
	}
	.blog__card a {
		display: block;
		height: 100%;
		padding: 0.85rem;
		border: 1px solid var(--line);
		border-radius: var(--radius-md, 0.75rem);
		background: var(--bg-panel);
		text-decoration: none;
		color: inherit;
	}
	.blog__card img {
		width: 100%;
		aspect-ratio: 16 / 9;
		object-fit: cover;
		margin-bottom: 0.75rem;
	}
	.blog__card h2 {
		margin: 0.35rem 0 0.5rem;
		font-size: 1.05rem;
		line-height: 1.25;
	}
	.blog__card p,
	.blog__feature-card p {
		margin: 0;
		color: rgba(255, 255, 255, 0.68);
		font-size: 0.92rem;
	}
	.blog__cat {
		display: inline-block;
		color: var(--magenta-soft);
		font-size: 0.72rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.06em;
	}
	.blog__more,
	.blog__meta {
		display: inline-block;
		margin-top: 0.75rem;
		color: rgba(255, 255, 255, 0.55);
		font-size: 0.8rem;
		font-weight: 600;
	}
	.blog__more {
		color: var(--magenta-soft);
	}
	@media (max-width: 760px) {
		.blog__feature-card {
			grid-template-columns: 1fr;
		}

		.blog__cheat-hub-list {
			grid-template-columns: 1fr;
		}

		.blog__list {
			grid-template-columns: 1fr;
			gap: 1rem;
		}

		.blog__card a {
			padding: 0.75rem;
		}
	}

	@media (max-width: 480px) {
		.blog__feature-card h2 {
			font-size: 1.2rem;
		}
	}
</style>
